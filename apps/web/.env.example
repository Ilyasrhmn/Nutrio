# API base URL (backend)
NEXT_PUBLIC_API_URL=http://localhost:3333

# Set to "true" only on demo deployments where backend runs with AI_MOCK=true
# or seeded/fixture data. Shows a persistent banner so demo data is never
# mistaken for a real user's account data.
NEXT_PUBLIC_DEMO_MODE=false
