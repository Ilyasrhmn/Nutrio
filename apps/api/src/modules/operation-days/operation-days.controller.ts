import { Body, Controller, Get, Headers, Param, Post, Req, UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { CreateOperationDayDto } from './dto/create-operation-day.dto'; import { OperationDaysService } from './operation-days.service';
@Controller('operation-days') @UseGuards(JwtAuthGuard) export class OperationDaysController { constructor(private readonly days:OperationDaysService){} @Get('today') today(@Req() req:any){return this.days.today(req.user.id)} @Post() create(@Req() req:any,@Body() dto:CreateOperationDayDto,@Headers('idempotency-key') key?:string){return this.days.create(req.user.id,dto.menuPlanId,key)} @Post(':id/close') close(@Req() req:any,@Param('id') id:string,@Headers('idempotency-key') key?:string){return this.days.close(req.user.id,id,key)} }
