/**
 * @workspace/contract-types
 *
 * This package holds shared ABI types and typed contract interfaces.
 * After compiling contracts with `forge build`, copy ABIs here and
 * create typed wrappers using viem's type utilities.
 *
 * Example usage:
 * ```ts
 * import { counterAbi } from "@workspace/contract-types";
 * import { getContract } from "viem";
 *
 * const contract = getContract({
 *   address: "0x...",
 *   abi: counterAbi,
 *   client,
 * });
 * ```
 */

// Placeholder — will be populated after contract compilation
// export { counterAbi } from "./abis/Counter.js";
